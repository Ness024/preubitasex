/*import { Component, OnInit } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-document-status-histories',
  imports: [
    CommonModule
  ],
  templateUrl: './document-status-histories.component.html',
  styleUrl: './document-status-histories.component.css'
})
export class DocumentStatusHistoriesComponent implements OnInit {
 documentNumber = 'DOC-2023-0568';
  expandedStep: string | null = 'in_signing'; // Paso expandido por defecto

  constructor() { }

  ngOnInit(): void {
    // Aquí iría la lógica para cargar los datos del historial del documento
  }

  toggleStep(stepId: string): void {
    if (this.expandedStep === stepId) {
      this.expandedStep = null;
    } else {
      this.expandedStep = stepId;
    }
  }
}
*/
